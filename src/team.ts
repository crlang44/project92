import { Formation } from "./Formation";
import { Player } from "./Player";
import { Side } from "./side";

export class Team {
    formation: Formation;
    players: Player[];

    constructor(side: Side){
        
    }
}